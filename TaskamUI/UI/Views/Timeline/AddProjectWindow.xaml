<Window x:Class="TaskamUI.UI.Views.Timeline.AddProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskamUI.UI.Views.Timeline"
        mc:Ignorable="d"
        Title="Create New Project" 
        Height="550" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Border Padding="20" Background="#F5F5F5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Create New Project"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#2C2C2C"
                       Margin="0,0,0,20"/>

            <!-- Project Name -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <TextBox x:Name="ProjectNameBox"
                         TextChanged="ProjectNameBox_TextChanged"/>
                <TextBlock x:Name="ProjectNamePlaceholder"
                           Text="Project Name"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="#999999"
                           FontSize="13"
                           IsHitTestVisible="False"/>
            </Grid>

            <!-- Start Date -->
            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <TextBlock Text="Start Date"
                           Foreground="#2C2C2C"
                           Margin="0,0,0,4"
                           FontSize="13"/>
                <DatePicker x:Name="StartDatePicker"/>
            </StackPanel>

            <!-- End Date -->
            <StackPanel Grid.Row="3" Margin="0,0,0,12">
                <TextBlock Text="End Date"
                           Foreground="#2C2C2C"
                           Margin="0,0,0,4"
                           FontSize="13"/>
                <DatePicker x:Name="EndDatePicker"/>
            </StackPanel>

            <!-- Buttons -->
            <Grid Grid.Row="6" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Cancel"
                        Click="Cancel_Click"/>

                <Button Grid.Column="2"
                        Content="Create Project"
                        Click="GetProjectInfo_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>